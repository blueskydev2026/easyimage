{
  "name": "easyimage-next",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/generate-icons.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node scripts/generate-icons.mjs && node tests/run-tests.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.1"
  }
}
